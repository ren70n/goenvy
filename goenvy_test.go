package goenvy

import "testing"

func Test_Getenvs(t *testing.T) {
	// want := "Goenvy"
	// if got := Goenvy("test.env.json"); got != want {
	// t.Errorf("Goenvy() = %q, want %q", got, want)
	// }
}
